<br>

<%= flash[:no_items] %><br>

<% @items.each do |item| %>

<%= link_to public_item_path(item.id) do %>
  <%= attachment_image_tag item, :image, :fill, 100,100, fallback: "no_image.png", size: "100x100" %><br>
<% end %>
  <%= item.name %><br>
  ¥<%= item.untax_price*108/100 %><br>
<% end %>